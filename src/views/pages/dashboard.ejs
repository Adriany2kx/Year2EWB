<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/dashboard.css">
        <link rel="stylesheet" href="/css/main.css">
        <link rel="stylesheet" href="/css/navbar.css">
        <link rel="stylesheet" href="/css/footer.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
        <title>Makers Hub</title>
    </head>
    <body>
        <%- include('../common/navigation.ejs') %>
        <main>
            <section class="dashboard">
                <div class="welcome-section">
                    <h1><%= dashboard.welcome %>, <%= user ? user.Username : 'Guest' %>!</h1>
                    <p class="welcome-subtitle">What would you like to do today?</p>
                </div>
                <div class="dashboard-options">
                    <div class="option">
                        <img src="/images/beer2.png" alt="<%= dashboard.browseJobs %>">
                        <h2><a href="/jobs"><%= dashboard.browseJobs %></a></h2>
                        <p><%= dashboard.browseJobsDescription %></p>
                    </div>
                    <div class="option">
                        <img src="/images/beer4.png" alt="<%= dashboard.joinVolunteering %>">
                        <h2><a href="/volunteering"><%= dashboard.joinVolunteering %></a></h2>
                        <p><%= dashboard.joinVolunteeringDescription %></p>
                    </div>
                    <div class="option">
                        <img src="/images/beer3.png" alt="<%= dashboard.upcomingWorkshops %>">
                        <h2><a href="/workshops"><%= dashboard.upcomingWorkshops %></a></h2>
                        <p><%= dashboard.upcomingWorkshopsDescription %></p>
                    </div>
                </div>
                <section class="activity">
                    <h2><%= dashboard.yourActivity %></h2>
                    <div class="activity-grid">
                        <div class="activity-item">
                            <i class="fas fa-file-alt fa-2x"></i>
                            <div class="activity-content">
                                <h3><%= dashboard.applicationsLabel %></h3>
                                <p><%= user && user.application ? user.application : 0 %></p>
                            </div>
                        </div>
                        <div class="activity-item">
                            <i class="fas fa-hands-helping fa-2x"></i>
                            <div class="activity-content">
                                <h3><%= dashboard.volunteersLabel %></h3>
                                <p><%= user && user.volunteers ? user.volunteers : 0 %></p>
                            </div>
                        </div>
                    </div>
                </section>
            </section>
        </main>
        <%- include('../common/footer.ejs') %>
    </body>
</html>
